class Teste4

int idade, j, i, total; // corrigido: identificador 'i' não declarado
float k; // corrigido: k recebe divisão, que sempre retorna float
string nome, sobrenome, texto; // corigido: 'sobrenome' não declarado

init
    write("Digite o seu nome: ");
    read(nome);
    write("Digite o seu sobrenome");
    read(sobrenome);
    write("Digite a sua idade: ");
    read (idade);
    k = i * (5-i * 50 / 10);
    j = i * 10;
    k = i * j / k;
    texto = nome + " " + sobrenome + ", os números gerados sao: ";
    write (texto); // corrigido: o nome da variável declarada é 'texto' e não 'text'
    write(j);
    write(k);
stop